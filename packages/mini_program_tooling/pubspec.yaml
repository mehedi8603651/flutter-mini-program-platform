name: mini_program_tooling
description: Local developer and CI tooling for the Flutter mini-program platform.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  http: ^1.5.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
