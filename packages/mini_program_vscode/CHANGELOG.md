## 0.4.0

- Remove external artifact delivery adapters and credential handoff commands from the active extension.
- Keep commands for create/build/validate/preview, static artifact publish, partner package import, host endpoint add/import, local mock Publisher API, and runtime contract smoke.
- Update diagnostics and status tree wording around public static artifacts and optional runtime middle-server APIs.
