name: partner_app_host
description: Reference Flutter host app for portable mini-program partner integration.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  mini_program_contracts:
    path: ../../packages/mini_program_contracts
  mini_program_sdk:
    path: ../../packages/mini_program_sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
