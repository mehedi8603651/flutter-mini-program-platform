# Food Order

Mp JSON mini-program fixture for a simple food ordering flow.

It covers:

- Mp-only layout, image, card, list, and button rendering
- internal mini-program navigation
- portable Mp JSON authoring only

Build it with:

```powershell
dart run ..\..\packages\mini_program_tooling\bin\miniprogram.dart build --mini-program-root .
```
