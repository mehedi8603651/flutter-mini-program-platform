name: food_order
description: Mp JSON food ordering mini-program fixture.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  mini_program_ui:
    path: ../../packages/mini_program_ui

dependency_overrides:
  mini_program_contracts:
    path: ../../packages/mini_program_contracts
