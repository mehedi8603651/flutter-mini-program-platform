# Mp Rewards Center

Tracked Mp JSON migration fixture for runtime parity coverage.

It covers:

- `Mp.authBuilder`
- `Mp.backendBuilder`
- `Mp.lazy.chunk`
- manual Load more with `Mp.lazy.loadMore`

Build it with:

```powershell
dart run ..\..\packages\mini_program_tooling\bin\miniprogram.dart build --mini-program-root .
```
